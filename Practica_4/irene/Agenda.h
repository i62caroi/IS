#ifndef AGENDA_H
#define AGENDA_H
/*
class Agenda {

private:
	list<alumno> Lista_alumno;
	char apellidos_;
	char dni_;
	int grupo_;

public:
	void Anadir_alumno(list<Alumno> &lista);
	void Editar_alumno(list<Alumno> &lista);
	void Eliminar_alumno(list<Alumno> &lista);
	void Mostrar_alumno(list<Alumno> &lista);
	void Listar_alumnos(list<Alumno> &lista);
	bool Buscar_apellidos(string apellidos_);
	bool Buscar_dni(string dni_);
	bool Buscar_grupo(string grupo_);
};
*/
#endif
